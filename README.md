# CRUDS
A console-based Java application that performs CRUDS operations (Create, Read, Update, Delete, Sort) on employee records. It is built with core Java concepts: OOP, inheritance, polymorphism, abstract classes, collections.
